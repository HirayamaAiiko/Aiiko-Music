# ==========================================
# AIIKO MUSIC - Core Requirements (コア要件)
# ==========================================
# このファイルには、開発用や推移的な依存関係を除き、
# プロジェクトの実行に必要な依存関係のみが含まれています。

# --- GUI Framework y Componentes (GUIフレームワークとコンポーネント) ---
PyQt6>=6.6.0,<=6.10.2
PyQt6-Fluent-Widgets>=1.6.0,<=1.11.1
PyQt6-Frameless-Window>=0.4.0,<=0.8.0
pyqtgraph>=0.13.0,<=0.14.0

# --- Manipulación de Audio y Media (オーディオおよびメディア処理) ---
mutagen>=1.46.0,<=1.47.0
numpy>=1.26.0,<=2.4.2

# --- Procesamiento de Imágenes (画像処理) ---
pillow>=10.0.0,<=12.1.1

# --- Interacción Nativa del Sistema Operativo (OSネイティブ統合 - Windows) ---
pywin32>=306,<=311
pynput>=1.7.6,<=1.8.1
Send2Trash>=2.1.0,<=2.1.0

# --- SMTC (System Media Transport Controls) ---
winrt-Windows.Media>=3.0.0
winrt-Windows.Media.Playback>=3.0.0
winrt-Windows.Foundation>=3.0.0
winrt-Windows.Storage.Streams>=3.0.0

# --- Monitoreo de Archivos y Recursos (ファイルとリソースの監視) ---
watchdog>=3.0.0,<=6.0.0
psutil>=5.9.0,<=7.2.2

# --- Integraciones y Red (ネットワークと統合) ---
pypresence>=4.3.0,<=4.6.1
requests>=2.31.0,<=2.32.5

# ==========================================
# Dependencias Desaprobadas / Obsoletas (非推奨 / 廃止された依存関係)
# ==========================================
# python-vlc  -> ネイティブのBASSエンジン(bass_bindings)に置き換えられました。
# PyQt5 / PySide6 -> コードは完全にPyQt6に移行しました。共存すると競合が発生します。
